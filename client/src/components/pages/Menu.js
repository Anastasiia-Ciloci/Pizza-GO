import React from "react";
// import { useQuery } from "@apollo/client";
// import { QUERY_PIZZAS } from "../utils/queries";
// import PizzaCard from "./pages/PizzaCard";

//need some logic to add to cart
//just UI below
const Menu = () => {
  // const { data } = useQuery(QUERY_PIZZAS);
  // const pizza = data?.pizzas || [];

  return (
    <>
      {/* for(pizza: pizzas) {<PizzaCard {...pizza} />}
  </>; */}
      {/* <PizzaCard /> */}
      <div>Hello i'm menu</div>
    </>
  );
};

export default Menu;
